plugins {
    id "java"
    id "maven-publish"
}

group = 'com.github.Anuken'

sourceCompatibility = targetCompatibility = 8

repositories {
    jcenter()
}

dependencies {
    compile 'net.bytebuddy:byte-buddy:1.10.16'
    compile 'net.bytebuddy:byte-buddy-agent:1.10.16'
}


task sourcesJar(type: Jar) {
    classifier 'sources'
    from sourceSets.main.allJava
}

javadoc {
    options.source = "8"
}

task javadocJar(type: Jar) {
    from javadoc
    classifier = 'javadoc'
}

publishing {
    publications {
        mavenJava(MavenPublication) { publication ->
            from components.java
            artifact sourcesJar
            artifact javadocJar
        }
    }
}